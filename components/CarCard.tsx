"use client";
import Link from "next/link";import { ArrowRight,Fuel,Gauge,Heart } from "lucide-react";import type{Car}from"@/data/cars";import{useLanguage}from"./LanguageProvider";
export default function CarCard({car}:{car:Car}){const{lang}=useLanguage();const sq=lang==="sq";return <Link href={`/veturat/${car.slug}`} className="car-card"><div className="car-image"><img src={car.image} alt={`${car.brand} ${car.model}`}/><span className={`status ${car.status==="E rezervuar"?"reserved":""}`}>{sq?car.status:car.status==="E rezervuar"?"Reserved":"Available"}</span><span className="heart"><Heart size={18}/></span></div><div className="car-content"><p>{car.brand}</p><h3>{car.model}</h3><div className="car-meta"><span>{car.year}</span><span><Gauge size={14}/>{car.mileage}</span><span><Fuel size={14}/>{car.fuel==="Benzinë"?(sq?"Benzinë":"Petrol"):car.fuel==="Hibrid"?(sq?"Hibrid":"Hybrid"):car.fuel}</span></div><div className="car-bottom"><strong>{car.price}</strong><span>{sq?"Detajet":"Details"} <ArrowRight size={16}/></span></div></div></Link>}
