"use client";
import { Check, ChevronDown } from "lucide-react";
import { useEffect,useRef,useState } from "react";
type Option={label:string;value:string;logo?:string};
export default function CustomSelect({label,placeholder,options,value,onChange,disabled=false}:{label:string;placeholder:string;options:Option[];value:string;onChange:(v:string)=>void;disabled?:boolean}){const[open,setOpen]=useState(false);const ref=useRef<HTMLDivElement>(null);const selected=options.find(o=>o.value===value);useEffect(()=>{const close=(e:MouseEvent)=>{if(ref.current&&!ref.current.contains(e.target as Node))setOpen(false)};document.addEventListener("mousedown",close);return()=>document.removeEventListener("mousedown",close)},[]);return <div className={`custom-select ${disabled?"is-disabled":""}`} ref={ref}><label>{label}</label><button type="button" disabled={disabled} className={`select-trigger ${open?"is-open":""}`} onClick={()=>setOpen(!open)}><span className="select-value">{selected?.logo&&<img src={selected.logo} alt=""/>}{selected?.label??placeholder}</span><ChevronDown size={16}/></button>{open&&<div className="select-menu">{options.map(o=><button type="button" key={o.value} className={value===o.value?"is-selected":""} onClick={()=>{onChange(o.value);setOpen(false)}}><span>{o.logo&&<img src={o.logo} alt=""/>}{o.label}</span>{value===o.value&&<Check size={15}/>}</button>)}</div>}</div>}
